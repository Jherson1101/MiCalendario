<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- permiso para enviar notificaciones al usuario -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiCalendario">
        <activity
            android:name=".activities.PerfilActivity"
            android:exported="false" />
        <!-- activity para mostrar reportes -->
        <activity
            android:name=".activities.ReporteActivity"
            android:exported="false" /> <!-- recibe las alarmas -->
        <receiver
            android:name=".notifications.NotificationReceiver"
            android:exported="false" /> <!-- pantalla para editar las tareas -->
        <activity
            android:name=".activities.EditTaskActivity"
            android:exported="false" /> <!-- pantalla para agregar actividades -->
        <activity
            android:name=".activities.AddTaskActivity"
            android:exported="false" /> <!-- activity para la visualizacion de tareas -->
        <activity
            android:name=".item_task"
            android:exported="false" /> <!-- pantalla principal de la app -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" /> <!-- pantalla de login -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>